{
  "name": "booking-app-backend",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server",
    "start-dev": "nodemon server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "colors": "^1.4.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "mongoose": "^6.10.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.12"
  },
  "description": "This is the backend for the booking app. It is a Node.js app using Express and MongoDB. It is deployed on vercel/render. It is a REST API. It is a CRUD app. It is a full stack app. It is a MERN app. pattern: MVC."
}
